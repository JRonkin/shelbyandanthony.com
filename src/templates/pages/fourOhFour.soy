{namespace page}

{template .fourOhFour}
  {@param assets: map<string, [
    height: int,
    orientation: int,
    path: uri,
    size: int,
    trustedUrl: uri,
    url: uri,
    width: int
  ]>}
  {@param baseUrl: uri}
  {@param hostname: uri}
  {@param pathname: uri}
  {@param protocol: string}

  {call common.layout data="all"}
    {param blockSearchEngines: true /}
    {param description: 'Join us in celebrating our wedding! RSVP, find travel information, view the itinerary, and browse our registry.' /}
    {param favicon: $assets['images/favicon.png'].url /}
    {param headIncludes kind="html"}
      <base href="/">
      <link rel="stylesheet" href="{$assets['fourOhFour.css'].url}">
      <script src="{$assets['fourOhFour.js'].trustedUrl}" defer></script>
    {/param}
    {param pageContent kind="html"}
      {call modules.Hero}
        {param title: 'Oops!' /}
      {/call}

      {call sections.fourOhFour.PageNotFound data="all" /}
    {/param}
    {param title: 'Page Not Found | Shelby & Anthony\'s Wedding' /}
  {/call}
{/template}
