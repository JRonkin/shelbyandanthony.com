{namespace page}

{template .itinerary}
  {@param assets: map<string, [
    height: int,
    orientation: int,
    path: uri,
    size: int,
    trustedUrl: uri,
    url: uri,
    width: int
  ]>}
  {@param baseUrl: uri}
  {@param blockSearchEngines: bool}
  {@param hostname: uri}
  {@param pathname: uri}
  {@param protocol: string}

  {call common.layout data="all"}
    {param description: 'Join us in celebrating our wedding! RSVP, find travel information, view the itinerary, and browse our registry.' /}
    {param favicon: $assets['images/favicon.png'].url /}
    {param headIncludes kind="html"}
      <link rel="stylesheet" href="{$assets['itinerary.css'].url}">
      <script src="{$assets['itinerary.js'].trustedUrl}" defer></script>
    {/param}
    {param pageContent kind="html"}
      {call modules.Hero}
        {param title: 'Itinerary' /}
      {/call}

      {call sections.itinerary.Itinerary data="all" /}
    {/param}
    {param title: 'Itinerary | Shelby & Anthony\'s Wedding' /}
  {/call}
{/template}
